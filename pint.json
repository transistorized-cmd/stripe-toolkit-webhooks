{
    "preset": "laravel",
    "exclude": [
        "vendor",
        "build",
        ".phpunit.cache"
    ],
    "rules": {
        "declare_strict_types": true,
        "ordered_imports": {
            "sort_algorithm": "alpha"
        }
    }
}
